# include <stdio.h>

	int divi(int x, int y,int z)
//	int mahi(int x,int y,int z)
	{
		return (x/y);
		return (x-(y*z));
	}
	int main(){
		int x=12,y=5,z,m;
		z= divi( x,y);
		m= mahi(x,y,z);
		printf(" quotient is %d",z);
		printf(" remainder is %d",m);
	return 0;
}
  int main(){
  	
  	int divi(int x,int y,int z, int m )
  	   
  	   if( x>y){
  	   	return ()
  	   	printf(" quotient is %d:");
  	   	
		 }
  }
