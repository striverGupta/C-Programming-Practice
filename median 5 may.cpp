#include <stdio.h>
#include <conio.h>
int main(){
	
	int i,j,arr[20];
	
	float median, values[10];
	scanf("%d",&n);
	
	printf("\n enter the values :");
	for(i=0;i<n;i++)
	scanf("%f",&value[i]);
	if (n % 2 ==0)
	 median = (values [n\2]+ values [n/2+1])/2.0;
	else 
    	median = values[n/2+1];
	printf("\n MEDIAN =%f ", median);
	getch();
	return 0;
}
	
	
	return 0;
}
