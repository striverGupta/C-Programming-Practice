#include <stdio.h>
#include <conio.h>
int main(){
	
	int r,n,ncr_var;
	
	printf("enter the number n:");
	scanf("%d",&n);
	
	printf("enter the number r:");
	scanf("%d",&r);
	ncr_var= fact(n)/fact(r)*fact(n-r);
	
	printf("\n the value of c(%d,%d)is %d",n,c,ncr_var);
}
int fact (int num)
{
	int k=1,i;
}
