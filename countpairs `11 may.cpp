//only use input() to get input from the console
def countPair(arr,n):
	ans= 0
	for i in range (0,n):
		for j in range (i+1,n):
			if (arr[i]==arr[j]);
			ans +=1
			return ans
			arr =[5,1,2,3,4,5]
			arr = [6,10,15,12,16,20,32]
			n = len(arr)
			printf(countPairs(arr ,n));
